# Local dev — copy to .env.local (do not use localhost in production hosting).
# NEXT_PUBLIC_API_URL=http://localhost:9400
# API_URL=http://localhost:9400

# Production (also see .env.production for this project's Render API).
# NEXT_PUBLIC_API_URL=https://threedwebsoft-lms-4.onrender.com
# API_URL=https://threedwebsoft-lms-4.onrender.com
